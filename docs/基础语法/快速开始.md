# 快速开始示例

让我们通过一些实用的C语言程序来快速入门：

## 1. Hello World

```c
#include <stdio.h>

int main() {
    printf("Hello, World!\n");
    return 0;
}
```

## 2. 计算器

```c
#include <stdio.h>

int main() {
    float a, b;
    char op;

    printf("输入表达式 (如: 5 + 3): ");
    scanf("%f %c %f", &a, &op, &b);

    switch(op) {
        case '+': printf("%.2f\n", a + b); break;
        case '-': printf("%.2f\n", a - b); break;
        case '*': printf("%.2f\n", a * b); break;
        case '/': printf("%.2f\n", a / b); break;
        default: printf("无效操作符\n");
    }

    return 0;
}
```

## 3. 猜数字游戏

```c
#include <stdio.h>
#include <stdlib.h>
#include <time.h>

int main() {
    srand(time(0));
    int num = rand() % 100 + 1;
    int guess;

    printf("猜数字 (1-100):\n");
    do {
        scanf("%d", &guess);
        if(guess > num) printf("太大\n");
        else if(guess < num) printf("太小\n");
    } while(guess != num);

    printf("恭喜猜对!\n");
    return 0;
}
```

现在让我们启动网站看看效果！